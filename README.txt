Reset - оновлює таблицю до початкового значення.

Task1(CreateTextFile) - створює файл в якому міститься пересортовані елементи.

Task2(CreateResultInText) - виводить вікно в якому слід вибрати файл(Task1_Input) елементи якого потрібно відсортувати 
			    і записує результат в файл Task2_output.txt.

Task3(ShowResult) - виводить вікно в якому слід вибрати файл(Task1_Input) елементи якого потрібно відсортувати 
                    і графічно показує результат.

MixPuzzle - перемішує елементи.